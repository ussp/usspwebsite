# Nixpacks configuration for Railway deployment (Next.js)

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["cd packages/platform-core && npx tsc && cd ../.. && next build"]

[start]
cmd = "npm start"
