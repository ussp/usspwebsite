# Nixpacks configuration for USSP public site
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm install", "cd packages/platform-core && npx tsc && cd ../.. && npm run build"]

[start]
cmd = "npm run start"
